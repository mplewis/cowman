- To install packages in this project, use pnpm install <package-name> to get the latest. Don't modify package.json with an arbitrary version.

- As you execute on the plan, check off the boxes for each step.
- You can use directory tmp/ to perform experiments such as demo scripts.