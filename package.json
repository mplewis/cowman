{
	"name": "cowman",
	"version": "1.0.0",
	"description": "",
	"main": "dist/index.js",
	"type": "module",
	"scripts": {
		"build": "tsc",
		"dev": "tsx watch src/index.ts",
		"start": "node dist/index.js",
		"test": "vitest",
		"test:run": "vitest run",
		"db:generate": "prisma generate",
		"db:migrate": "prisma migrate dev",
		"db:deploy": "prisma migrate deploy",
		"db:studio": "prisma studio",
		"check": "pnpm test:run && pnpm typecheck && pnpm lint",
		"check:fix": "pnpm test:run && pnpm typecheck && pnpm lint:fix",
		"typecheck": "tsc --noEmit",
		"lint": "biome check . && prettier --check .",
		"lint:fix": "biome check --write . && prettier --write ."
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"packageManager": "pnpm@10.14.0",
	"dependencies": {
		"@prisma/client": "^6.14.0",
		"@types/node": "^24.3.0",
		"discord.js": "^14.22.1",
		"natural": "^8.1.0",
		"pino": "^9.9.0",
		"pino-pretty": "^13.1.1",
		"prisma": "^6.14.0",
		"tsx": "^4.20.5",
		"typescript": "^5.9.2",
		"vitest": "^3.2.4"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.2",
		"prettier": "^3.6.2"
	}
}
